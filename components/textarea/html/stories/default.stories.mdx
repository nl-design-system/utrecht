{/* @license CC0-1.0 */}

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, Textarea } from "../story-template";

<Meta
  id="html-textarea"
  title="HTML Component/Textarea"
  component={Textarea}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Textarea component

Styling via the `<textarea>` element:

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {Textarea.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
