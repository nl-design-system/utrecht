<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs, TextArea } from "./bem";
import "./bem.scss";

<Meta
  title="CSS Component/Text area"
  argTypes={{
    disabled: {
      description: "Disabled",
      control: "boolean",
    },
    focus: {
      description: "Focus",
      control: "boolean",
    },
    invalid: {
      description: "Invalid",
      control: "boolean",
    },
    readOnly: {
      description: "Read-only",
      control: "boolean",
    },
    required: {
      description: "Required",
      control: "boolean",
    },
    textContent: {
      description: "Set the value of the Text area",
      control: "text",
    },
  }}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Text area component

Styling via the `.utrecht-textarea` class name.

<Canvas>
  <Story
    name="Text area"
    args={{
      ...defaultArgs,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Text area" />

## States

### Disabled

<Canvas>
  <Story
    name="Disabled Text area"
    args={{
      disabled: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

### Invalid

<Canvas>
  <Story
    name="Invalid Text area"
    args={{
      invalid: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

### Read-only

<Canvas>
  <Story
    name="Read-only Text area"
    args={{
      readOnly: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

### Required

<Canvas>
  <Story
    name="Required Text area"
    args={{
      required: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

## Interactive States

### Focus

<Canvas>
  <Story
    name="Text area with focus"
    args={{
      focus: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

### Hover

<Canvas>
  <Story
    name="Text area with hover state"
    args={{
      hover: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>
