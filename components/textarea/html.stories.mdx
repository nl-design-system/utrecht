<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs, TextArea } from "./html";
import "./html.scss";

<Meta
  title="HTML Component/Textarea"
  argTypes={{
    invalid: {
      description: "Invalid",
      control: "boolean",
    },
    readOnly: {
      description: "Read-only",
      control: "boolean",
    },
    disabled: {
      description: "Disabled",
      control: "boolean",
    },
    placeholder: {
      description: "Placeholder",
      control: "text",
    },
    required: {
      description: "Required",
      control: "boolean",
    },
    textContent: {
      description: "Set the content of the textarea",
      control: "text",
    },
  }}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => TextArea(args),
    },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Textarea component

Styling via the `<textarea>` element:

<Canvas>
  <Story
    name="Textarea"
    args={{
      ...defaultArgs,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Textarea" />

## States

### Invalid

<Canvas>
  <Story
    name="Invalid textarea"
    args={{
      invalid: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    name="Disabled textarea"
    args={{
      disabled: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

### Read-only

<Canvas>
  <Story
    name="Read-only textarea"
    args={{
      readOnly: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

### Required

<Canvas>
  <Story
    name="Required textarea"
    args={{
      required: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

## Placeholder

<Canvas>
  <Story
    name="Textarea with placeholder"
    args={{
      placeholder: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>
