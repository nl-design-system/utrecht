<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { FormLabel } from "../template";
import "../index.scss";

<Meta
  id="css-form-label"
  title="CSS Component/Form label"
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Label component for disabled input

Styling via the `.utrecht-form-label--disabled` class name:

<Canvas>
  <Story
    name="--disabled"
    args={{
      disabled: true,
      textContent: "Username",
    }}
  >
    {FormLabel.bind({})}
  </Story>
</Canvas>
