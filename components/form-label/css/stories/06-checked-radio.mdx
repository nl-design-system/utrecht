<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { FormLabel } from "../template";
import "../index.scss";

<Meta
  id="css-form-label"
  title="CSS Component/Form label"
  argTypes={{
    textContent: {
      description: "Set the content of the label",
      control: "text",
    },
    type: {
      description: "Set the type of the form control",
      options: ["default", "checkbox", "radio"],
      control: { type: "radio" },
    },
  }}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Label component for checked radio button

Styling via the `.utrecht-form-label--radio` and `.utrecht-form-label--checked` class names:

<Canvas>
  <Story
    name="--checked --radio"
    args={{
      checked: true,
      textContent: "Option 1",
      type: "radio",
    }}
  >
    {FormLabel.bind({})}
  </Story>
</Canvas>
