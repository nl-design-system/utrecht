{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, FormLabel } from "../story-template";
import "../index.scss";

<Meta
  id="css-form-label--type"
  title="CSS Component/Form label/Type"
  component={FormLabel}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Label component types

## For checkbox

Styling via the `.utrecht-form-label--checkbox` class name:

<Canvas>
  <Story
    name="Checkbox"
    args={{
      ...exampleArgs,
      type: "checkbox",
    }}
  >
    {FormLabel.bind({})}
  </Story>
</Canvas>

## For radio button

Styling via the `.utrecht-form-label--radio` class name:

<Canvas>
  <Story
    name="Radio"
    args={{
      textContent: "Option 1",
      type: "radio",
    }}
  >
    {FormLabel.bind({})}
  </Story>
</Canvas>
