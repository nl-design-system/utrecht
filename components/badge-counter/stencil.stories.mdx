<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";

export const Template = ({ locale = null, textContent = "", value = null, max = null }) =>
  `<utrecht-badge-counter${typeof max !== "undefined" && max !== null ? ` max="${max}"` : ""}${
    typeof value !== "undefined" && value !== null ? ` value="${value}"` : ""
  }${locale ? ` locale="${locale}"` : ""}>${textContent}</utrecht-badge-counter>`;

<Meta
  title="Web Component/Badge/Counter Badge"
  argTypes={{
    textContent: {
      description: "Text content",
      control: "text",
    },
    value: {
      description: "Value",
      control: "number",
    },
    max: {
      description: "Maximum",
      control: "number",
    },
    locale: {
      description: "Locale",
      control: "text",
    },
  }}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => Template(args),
    },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Counter badge

<Canvas>
  <Story
    name="Counter badge: 1 digit"
    args={{
      textContent: "7",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Counter badge: 2 digits"
    args={{
      textContent: "42",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Counter badge: 3 digits"
    args={{
      textContent: "316",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Counter badge: 4 digits"
    args={{
      textContent: "2059",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Counter badge: 1 digit" />

## Counter badge with maximum boundary

<Canvas>
  <Story
    name="Counter badge with maximum"
    args={{
      max: 999,
      value: 1024,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Counter badge with number formatted text content

<Canvas>
  <Story
    name="Counter badge with number formatted text content"
    args={{
      textContent: "1,234",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Counter badge with automatic number formatting

<Canvas>
  <Story
    name="Counter badge with automatic number formatting: US English"
    args={{
      textContent: 1234,
      value: 1234,
      locale: "en-US",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Counter badge with automatic number formatting: Finnish"
    args={{
      textContent: 1234,
      value: 1234,
      locale: "fi",
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Counter badge with automatic number formatting: Dutch"
    args={{
      textContent: 1234,
      value: 1234,
      locale: "nl-NL",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
