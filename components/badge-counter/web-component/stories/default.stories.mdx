<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, BadgeCounter, defaultArgs } from "../story-template";

<Meta
  id="web-component-counter-badge"
  title="Web Component/Badge/Counter Badge"
  argTypes={argTypes}
  component={BadgeCounter}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Counter badge

<Canvas>
  <Story name="Default" args={defaultArgs}>
    {BadgeCounter.bind({})}
  </Story>
  <Story name="Double Digit" args={{ value: 42, textContent: "42" }}>
    {BadgeCounter.bind({})}
  </Story>
  <Story name="Triple Digit" args={{ value: 316, textContent: "316" }}>
    {BadgeCounter.bind({})}
  </Story>
  <Story name="Quadruple Digit" args={{ value: 2148, textContent: "2148" }}>
    {BadgeCounter.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
