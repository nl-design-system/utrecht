<!--
@license EUPL-1.2
Copyright (c) 2021 Robbert Broersma
Copyright (c) 2021 Gemeente Utrecht
-->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { ToptaskCard, defaultArgs } from "./bem";
import iconSet from "../icon/icon-set.json";
import "./bem.scss";

<Meta
  title="CSS / Toptask Card"
  argTypes={{
    textContent: {
      description: "Card text",
      control: "text",
    },
    focus: {
      description: "Focus",
      control: "boolean",
    },
    focusVisible: {
      description: "Focus visible",
      control: "boolean",
    },
    hover: {
      description: "Hover",
      control: "boolean",
    },
    icon: {
      description: "Icon",
      control: { type: "select" },
      options: ["", ...iconSet],
    },
    disabled: {
      description: "Disabled",
      control: "boolean",
    },
  }}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => ToptaskCard(args),
    },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Toptask Card

Styling via the `.utrecht-toptask-card` class name:

<Canvas>
  <Story
    name="ToptaskCard"
    args={{
      ...defaultArgs,
      icon: "utrecht-icon-afval",
      textContent: "Het voordeel van structuur",
    }}
  >
    {ToptaskCard.bind({})}
  </Story>
</Canvas>

<ArgsTable story="ToptaskCard" />

## States

### Active

Styling via the `.utrecht-toptask-card--active` class name:

<Canvas>
  <Story
    name="ToptaskCard active"
    args={{
      active: true,
      icon: "utrecht-icon-afval",
      textContent: "Meer teskt op Card",
    }}
  >
    {ToptaskCard.bind({})}
  </Story>
</Canvas>

### Hover

Styling via the `.utrecht-toptask-card--hover` class name:

<Canvas>
  <Story
    name="ToptaskCard hover"
    args={{
      icon: "utrecht-icon-afval",
      textContent: "Read more...",
      hover: true,
    }}
  >
    {ToptaskCard.bind({})}
  </Story>
</Canvas>

### Focus

Styling via the `.utrecht-toptask-card--focus` class name:

<Canvas>
  <Story
    name="ToptaskCard focus"
    args={{
      icon: "utrecht-icon-afval",
      textContent: "Read more...",
      focus: true,
    }}
  >
    {ToptaskCard.bind({})}
  </Story>
</Canvas>

### Focus visible

Styling via the `.utrecht-toptask-card--focus-visible` class name:

<Canvas>
  <Story
    name="ToptaskCard focus visible"
    args={{
      icon: "utrecht-icon-afval",
      textContent: "Read more...",
      focus: true,
      focusVisible: true,
    }}
  >
    {ToptaskCard.bind({})}
  </Story>
</Canvas>

### Disabled

Styling via the `.utrecht-toptaks-card--disabled` class name:

<Canvas>
  <Story
    name="ToptaskCard disabled"
    args={{
      icon: "utrecht-icon-afval",
      textContent: "Read more...",
      disabled: true,
    }}
  >
    {ToptaskCard.bind({})}
  </Story>
</Canvas>
