<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Backdrop, checkerboard, defaultArgs } from "../template";
import "../index.scss";

<Meta
  id="css-backdrop"
  title="CSS Component/Backdrop"
  argTypes={{
    viewport: {
      name: "Cover viewport",
      type: "boolean",
    },
  }}
  decorators={[checkerboard]}
  parameters={{
    layout: "fullscreen",
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Backdrop

Styling via the `.utrecht-backdrop` class name. Displayed on a black-and-white checkerboard, so you can see the effect of the configured opacity.

<Canvas>
  <Story name="Backdrop" args={{ ...defaultArgs }}>
    {Backdrop.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Backdrop" />
