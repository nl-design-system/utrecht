<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, FormFieldTextarea } from "../story-template";

<Meta
  id="web-component-form-field-textarea"
  title="Web Component/Form field with textarea"
  component={FormFieldTextarea}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form field with textarea

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {FormFieldTextarea.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
