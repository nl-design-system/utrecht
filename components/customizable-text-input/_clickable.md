<!-- @license CC0-1.0 -->

Door een `label` met `for` en `aria-hidden` attributen kan de focus verlegd worden naar het `input` veld als je op de slots klikt. Door `aria-hidden` heeft dit label geen semantische waarde en wordt het verborgen voor screen readers.
