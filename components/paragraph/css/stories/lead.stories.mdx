{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, Paragraph } from "../story-template";
import "../index.scss";

<Meta
  id="css-paragraph"
  title="CSS Component/Paragraph/Lead"
  component={Paragraph}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Lead paragraph

Styling via the `.utrecht-paragraph` and `.utrecht-paragraph--lead` modifier class name:

<Canvas>
  <Story
    name="Lead"
    args={{
      ...exampleArgs,
      lead: true,
    }}
  >
    {Paragraph.bind({})}
  </Story>
</Canvas>
