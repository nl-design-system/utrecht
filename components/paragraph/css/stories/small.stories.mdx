{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, Paragraph } from "../story-template";
import "../index.scss";

<Meta
  id="css-paragraph"
  title="CSS Component/Paragraph"
  component={Paragraph}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Small print paragraph

Styling via the `.utrecht-paragraph` and `.utrecht-paragraph--small` modifier class name:

<Canvas>
  <Story
    name="Small print"
    args={{
      ...exampleArgs,
      small: true,
    }}
  >
    {Paragraph.bind({})}
  </Story>
</Canvas>
