{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, Paragraph } from "../story-template";

<Meta
  id="html-paragraph"
  title="HTML Component/Paragraph/Lead"
  component={Paragraph}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Lead paragraph

Styling via the `<p>` element with the `lead` class name:

<Canvas>
  <Story
    name="Lead"
    args={{
      ...exampleArgs,
      lead: true,
    }}
  >
    {Paragraph.bind({})}
  </Story>
</Canvas>
