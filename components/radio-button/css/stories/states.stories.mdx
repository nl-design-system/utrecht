{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { RadioButton } from "../story-template";
import "../index.scss";

<Meta
  id="css-radio-button--state"
  title="CSS Component/ Radio Button/States"
  component={RadioButton}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# radio button component states

## Hover

<Canvas>
  <Story
    name="hover"
    args={{
      hover: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Focus

<Canvas>
  <Story
    name="focus"
    args={{
      focus: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Focus-visible

<Canvas>
  <Story
    name="focus-visible"
    args={{
      focus: true,
      focusVisible: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Active

When the component is `active`, it always has `focus` too. Test these states together for accurate results.

<Canvas>
  <Story
    name="active"
    args={{
      active: true,
      focus: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      disabled: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Disabled + focus

<Canvas>
  <Story
    name="Disabled + focus"
    args={{
      disabled: true,
      focus: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Disabled + focus-visible

<Canvas>
  <Story
    name="Disabled + focus-visible"
    args={{
      disabled: true,
      focus: true,
      focusVisible: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Disabled + hover

<Canvas>
  <Story
    name="Disabled + hover"
    args={{
      disabled: true,
      hover: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Disabled + active

<Canvas>
  <Story
    name="Disabled + active"
    args={{
      disabled: true,
      active: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked

<Canvas>
  <Story
    name="Checked"
    args={{
      checked: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked + focus

<Canvas>
  <Story
    name="Checked and focus"
    args={{
      checked: true,
      focus: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked + focus-visible

<Canvas>
  <Story
    name="Checked and focus-visible"
    args={{
      checked: true,
      focus: true,
      focusVisible: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked + hover

<Canvas>
  <Story
    name="Checked and hover"
    args={{
      checked: true,
      hover: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked + active

<Canvas>
  <Story
    name="Checked and active"
    args={{
      checked: true,
      active: true,
      focus: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked + disabled

<Canvas>
  <Story
    name="Checked and Disabled"
    args={{
      checked: true,
      disabled: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked + disabled + hover

Should be identical to "Checked + disabled"

<Canvas>
  <Story
    name="Checked and Disabled and hover"
    args={{
      checked: true,
      disabled: true,
      hover: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked + disabled + focus

Should be identical to "Checked + disabled".

<Canvas>
  <Story
    name="Checked and Disabled and focus"
    args={{
      checked: true,
      disabled: true,
      focus: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked + disabled + focus-visible

Must have a focus ring.

<Canvas>
  <Story
    name="Checked and Disabled and focus-visible"
    args={{
      checked: true,
      disabled: true,
      focus: true,
      focusVisible: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked + disabled + active

Should be identical to "Checked + disabled".

<Canvas>
  <Story
    name="Checked and Disabled and active"
    args={{
      active: true,
      checked: true,
      disabled: true,
      focus: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Invalid

<Canvas>
  <Story
    name="invalid"
    args={{
      invalid: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>

## Checked + Invalid

<Canvas>
  <Story
    name="Checked + invalid"
    args={{
      checked: true,
      invalid: true,
    }}
  >
    {RadioButton.bind({})}
  </Story>
</Canvas>
