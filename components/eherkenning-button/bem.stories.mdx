<!--
@license EUPL-1.2
Copyright (c) 2021 Robbert Broersma
-->

import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { defaultArgs, EherkenningButton } from "./bem";

import "./bem.scss";

<Meta
  title="CSS Component/eHerkenning Button"
  argTypes={{
    label: {
      description: "Button label",
      control: "text",
    },
    textContent: {
      description: "Button text",
      control: "text",
    },
  }}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => EherkenningButton(args),
    },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Eherkenning button

Styling via the `.utrecht-eherkenning-button` class name:

<Canvas>
  <Story
    name="eHerkenning Button"
    args={{
      ...defaultArgs,
      label: "Inloggen eHerkenning",
      textContent: "Inloggen eHerkenning",
    }}
  >
    {EherkenningButton.bind({})}
  </Story>
</Canvas>

<ArgsTable story="eHerkenning Button" />

## Right-to-left

<Canvas>
  <Story
    name="eHerkenning Button in right-to-left text direction"
    decorators={[(story) => `<div dir="rtl" lang="ar">${story()}</div>`]}
    args={{
      ...defaultArgs,
      label: "سجّل الدخول إلى eHerkenning",
      textContent: "سجّل الدخول إلى eHerkenning",
    }}
  >
    {EherkenningButton.bind({})}
  </Story>
</Canvas>
