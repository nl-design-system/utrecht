{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, Heading3 } from "../story-template";

import "../index.scss";

<Meta
  id="css-heading-3--distanced"
  title="CSS Component/Heading/Heading 3/Distanced"
  component={Heading3}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Heading 3 distanced

Styling via `utrecht-heading-3` and `utrecht-heading-3--distanced` class names:

<Canvas>
  <Story
    name="Distanced"
    args={{
      ...exampleArgs,
      distanced: true,
    }}
  >
    {Heading3.bind({})}
  </Story>
</Canvas>
