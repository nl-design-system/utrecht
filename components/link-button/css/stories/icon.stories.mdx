{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { LinkButton } from "../story-template";
import "../index.scss";

<Meta
  id="css-link-button--icon"
  title="CSS Component/Button that looks like a link"
  component={LinkButton}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Link that looks like a button with icon

<Canvas>
  <Story
    name="with icon"
    decorators={[(Story) => <p className="utrecht-paragraph">{Story()}</p>]}
    args={{
      innerHTML: `Next <utrecht-icon-arrow></utrecht-icon-arrow>`,
    }}
  >
    {LinkButton.bind({})}
  </Story>
</Canvas>
