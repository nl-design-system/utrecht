<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, LinkButton } from "../story-template";
import "../index.scss";

<Meta
  id="css-link-button"
  title="CSS Component/Button that looks like a link/States"
  component={LinkButton}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Link button: button that looks like a link

## Hover state

Styling met de `.utrecht-link-button--hover` class naam of als state op link met `.utrecht-link-button:hover`:

<Canvas>
  <Story
    name="Hover"
    args={{
      ...exampleArgs,
      hover: true,
    }}
  >
    {LinkButton.bind({})}
  </Story>
</Canvas>

## Focus state

Styling met de `.utrecht-link-button--focus` class naam:

<Canvas>
  <Story
    name="Focus"
    args={{
      ...exampleArgs,
      focus: true,
    }}
  >
    {LinkButton.bind({})}
  </Story>
</Canvas>

## Focus-visible state

Styling met de `.utrecht-link-button--focus-visible` class naam of als status op `.utrecht-link-button:focus-visible`:

<Canvas>
  <Story
    name="Focus-visible"
    args={{
      ...exampleArgs,
      focus: true,
      focusVisible: true,
    }}
  >
    {LinkButton.bind({})}
  </Story>
</Canvas>

### Active state

Styling met de `.utrecht-link-button--active` class naam of als status op `.utrecht-link-button:active`:

<Canvas>
  <Story
    name="Active"
    args={{
      ...exampleArgs,
      active: true,
    }}
  >
    {LinkButton.bind({})}
  </Story>
</Canvas>

### Pressed state

Styling met de `.utrecht-link-button--pressed` class naam.

<Canvas>
  <Story
    name="Pressed"
    args={{
      ...exampleArgs,
      pressed: true,
    }}
  >
    {LinkButton.bind({})}
  </Story>
</Canvas>
