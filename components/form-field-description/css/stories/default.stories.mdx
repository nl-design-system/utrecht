{/* @license CC0-1.0 */}

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Paragraph } from "../../../paragraph/css/story-template";
import { argTypes, defaultArgs, FormFieldDescription } from "../story-template";
import "../index.scss";

<Meta
  id="css-form-field-description"
  title="CSS Component/Form field description"
  component={FormFieldDescription}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form field description

Combined with the form label, provide an additional and more detailed description of the form field.

<Canvas>
  <Story
    name="Default"
    args={{
      id: "0427f759-009c-43ad-8e1c-3d7b9982a133",
      children: <Paragraph>Your password must contain at least 16 characters.</Paragraph>,
    }}
  >
    {FormFieldDescription.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
