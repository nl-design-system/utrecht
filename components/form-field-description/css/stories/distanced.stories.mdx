{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, FormFieldDescription } from "../story-template";
import "../index.scss";

<Meta
  id="css-form-field-description"
  title="CSS Component/Form field description/Distanced"
  component={FormFieldDescription}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Valid Form field description

Describe that or why the form input is valid.

<Canvas>
  <Story
    name="Distanced"
    args={{
      ...exampleArgs,
      distanced: true,
    }}
  >
    {FormFieldDescription.bind({})}
  </Story>
</Canvas>
