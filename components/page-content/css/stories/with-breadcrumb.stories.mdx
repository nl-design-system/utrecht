{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/blocks";
import * as ReactDOMServer from "react-dom/server";
import { BreadcrumbNav } from "../../../../packages/component-library-react/src";
import { exampleArgs, PageContent } from "../story-template";

import "../index.scss";

<Meta
  id="css-page-content--breadcrumb-nav"
  title="CSS Component/Page Content/With Breadcrumb"
  component={PageContent}
  parameters={{
    status: "WORK IN PROGRESS",
  }}
/>

# Page Content with breadcrumb navigation

<Canvas>
  <Story
    name="With Breadcrumb"
    args={{
      ...exampleArgs,
      navHTML: ReactDOMServer.renderToStaticMarkup(
        BreadcrumbNav({ label: "", items: [], microdata: false, variant: "" })
      ),
    }}
  >
    {PageContent.bind({})}
  </Story>
</Canvas>
