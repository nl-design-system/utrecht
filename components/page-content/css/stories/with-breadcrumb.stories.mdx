<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import * as ReactDOMServer from "react-dom/server";
import { Breadcrumb, exampleArgs as breadcrumbArgs } from "../../../breadcrumb/css/story-template";
import { exampleArgs, PageContent } from "../story-template";

import "../index.scss";

<Meta
  id="css-page-content"
  title="CSS Component/Page Content/With Breadcrumb"
  component={PageContent}
  parameters={{
    status: "WORK IN PROGRESS",
  }}
/>

# Page Content with breadcrumb navigation

<Canvas>
  <Story
    name="With Breadcrumb"
    args={{
      ...exampleArgs,
      navHTML: ReactDOMServer.renderToStaticMarkup(Breadcrumb({ ...breadcrumbArgs })),
    }}
  >
    {PageContent.bind({})}
  </Story>
</Canvas>
