<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { AlertDialog, argTypes, defaultArgs, exampleArgs } from "../story-template";
import "../index.scss";

<Meta
  id="css-alert-dialog"
  title="CSS Component/Alert dialog"
  component={AlertDialog}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Alert dialog

Styling via the `.utrecht-alert-dialog` class name:

<Canvas>
  <Story name="Default" args={exampleArgs} height="320px">
    {AlertDialog.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
