<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Blockquote } from "../story-template";
import "../index.scss";

<Meta
  id="css-blockquote"
  title="CSS Component/Blockquote/Distanced"
  component={Blockquote}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Blockquote distanced

Styling via the `.utrecht-blockquote--distanced` class name:

<Canvas>
  <Story
    name="Distanced"
    args={{
      distanced: true,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {Blockquote.bind({})}
  </Story>
</Canvas>
