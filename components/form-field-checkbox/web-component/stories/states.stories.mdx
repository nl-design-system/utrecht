<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { FormFieldCheckbox } from "../template";

<Meta
  id="web-component-form-field-checkbox"
  title="Web Component/Form field with checkbox/States"
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form field with checkbox states

## Checked

<Canvas>
  <Story
    name="checked"
    args={{
      checked: true,
      label: "I agree",
    }}
  >
    {FormFieldCheckbox.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story
    name="disabled"
    args={{
      disabled: true,
      label: "I agree",
    }}
  >
    {FormFieldCheckbox.bind({})}
  </Story>
</Canvas>

## Invalid

<Canvas>
  <Story
    name="invalid"
    args={{
      invalid: true,
      label: "I agree",
    }}
  >
    {FormFieldCheckbox.bind({})}
  </Story>
</Canvas>

## Required

<Canvas>
  <Story
    name="required"
    args={{
      required: true,
      label: "I agree",
    }}
  >
    {FormFieldCheckbox.bind({})}
  </Story>
</Canvas>
