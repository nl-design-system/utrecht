<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import "./html.scss";

export const Template = () =>
  `<section class="utrecht-html">
  <ol>
    <li>Lorem</li>
    <li>Ipsum</li>
    <li>Dolor</li>
  </ol>
</section>`;

export const TemplateArabic = () =>
  `<section class="utrecht-html" lang="ar" dir="rtl">
  <ol>
    <li>واحد</li>
    <li>إثنان</li>
    <li>ثلاثة</li>
  </ol>
</section>`;

<Meta
  title="HTML Component/Ordered List"
  argTypes={{}}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => Template(args),
    },
    status: "IN DEVELOPMENT",
  }}
/>

## Ordered list

<Canvas>
  <Story name="Ordered list">{Template.bind({})}</Story>
</Canvas>

## Ordered list in Arabic

<Canvas>
  <Story
    name="Ordered list in Arabic"
    parameters={{
      docs: {
        transformSource: (_src, { args }) => TemplateArabic(args),
      },
    }}
  >
    {TemplateArabic.bind({})}
  </Story>
</Canvas>
