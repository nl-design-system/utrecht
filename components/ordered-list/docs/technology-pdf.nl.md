<!-- @license CC0-1.0 -->

# PDF

Ordered List moet opgebouwd worden met de volgende tags: `<L>`, `<LI>`, `<Lbl>` en `<LBody>`.

```text
<L> {
  <LI> {
    <Lbl> {
        "1."
    }
    <LBody> {
        "List item number 1"
    }
  }
}
```
