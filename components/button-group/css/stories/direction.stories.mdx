<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { ButtonGroup, exampleArgs } from "../story-template";
import "../index.scss";

<Meta
  id="css-button-group"
  title="CSS Component/Button group/Direction"
  component={ButtonGroup}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button group directions

## Horizontal

Styling via the `.utrecht-button-group` and `.utrecht-button-group--horizontal` modifier class names:

<Canvas>
  <Story
    name="Horizontal"
    args={{
      ...exampleArgs,
      direction: "horizontal",
    }}
  >
    {ButtonGroup.bind({})}
  </Story>
</Canvas>

## Vertical

Styling via the `.utrecht-button-group` and `.utrecht-button-group--vertical` modifier class names:

<Canvas>
  <Story
    name="Vertical"
    args={{
      ...exampleArgs,
      direction: "vertical",
    }}
  >
    {ButtonGroup.bind({})}
  </Story>
</Canvas>
