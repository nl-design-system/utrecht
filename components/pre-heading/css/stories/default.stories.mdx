{/* @license CC0-1.0 */}

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, PreHeading } from "../story-template";
import "../index.scss";

<Meta
  id="css-pre-heading"
  title="CSS Component/Pre-heading"
  component={PreHeading}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Pre-heading

Styling via `utrecht-pre-heading` class name:

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {PreHeading.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
