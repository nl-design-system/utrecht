<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs, PreHeading } from "../template";
import "../index.scss";

<Meta
  id="css-pre-heading"
  title="CSS Component/Pre-heading"
  argTypes={{
    textContent: {
      description: "Set the content of the heading",
      control: "text",
    },
    distanced: {
      description: "Distance the element from adjacent content",
      control: "boolean",
    },
  }}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Pre-heading

Styling via `utrecht-pre-heading` class name:

<Canvas>
  <Story
    name="Default"
    args={{
      ...defaultArgs,
      textContent: "The Quick Brown Fox Jumps Over The Lazy Dog",
    }}
  >
    {PreHeading.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
