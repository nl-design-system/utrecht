<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, Select } from "../story-template";
import "../index.scss";

<Meta
  id="css-select"
  title="CSS Component/Select"
  component={Select}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form select

Styling via the `<select>` element:

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {Select.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
