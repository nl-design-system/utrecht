{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, Select } from "../story-template";

<Meta
  id="html-select--state"
  title="HTML Component/Select/State"
  component={Select}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
    docs: {
      source: {
        state: "closed",
      },
    },
  }}
/>

# Form Select States

## Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      ...exampleArgs,
      disabled: true,
    }}
  >
    {Select.bind({})}
  </Story>
</Canvas>

## Invalid

<Canvas>
  <Story
    name="Invalid"
    args={{
      ...exampleArgs,
      invalid: true,
    }}
  >
    {Select.bind({})}
  </Story>
</Canvas>

## Required

<Canvas>
  <Story
    name="Required"
    args={{
      ...exampleArgs,
      required: true,
    }}
  >
    {Select.bind({})}
  </Story>
</Canvas>
