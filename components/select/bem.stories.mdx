<!--
@license EUPL-1.2
Copyright (c) 2021 Robbert Broersma
-->

import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import "./html.scss";
import clsx from "clsx";

export const Template = ({
  disabled = false,
  focus = false,
  invalid = false,
  options = [],
  readOnly = false,
  required = false,
}) =>
  `<select${invalid ? ' aria-invalid="true"' : ""}${disabled ? " disabled" : ""}${readOnly ? " readonly" : ""}${
    required ? " required" : ""
  } class="${clsx("utrecht-select", {
    "utrecht-select--disabled": disabled,
    "utrecht-select--focus": focus,
    "utrecht-select--invalid": invalid,
    "utrecht-select--required": required,
    "utrecht-select--read-only": readOnly,
  })}">
  ${options
    .map(
      ({ label, selected, value }) =>
        `<option${selected ? " selected" : ""}${value ? ` value="${value}"` : ""}>${label}</option>`
    )
    .join("\n  ")}
</select>`;

export const exampleOptions = [
  { value: "1", label: "Option #1" },
  { value: "2", label: "Option #2", selected: true },
  { value: "3", label: "Option #3" },
];

<Meta
  title="CSS Component/Form Select"
  argTypes={{
    disabled: {
      description: "Disabled",
      control: "boolean",
    },
    focus: {
      description: "Focus",
      control: "boolean",
    },
    invalid: {
      description: "Invalid",
      control: "boolean",
    },
    options: {
      description: "Options",
      control: "array",
    },
    readOnly: {
      description: "Read-only",
      control: "boolean",
    },
    required: {
      description: "Required",
      control: "boolean",
    },
  }}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => Template(args),
    },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form select

Styling via the `<select>` element:

<Canvas>
  <Story
    name="Select"
    args={{
      disabled: false,
      invalid: false,
      focus: false,
      options: exampleOptions,
      readOnly: false,
      required: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Select" />

## States

### Disabled

<Canvas>
  <Story
    name="Disabled select"
    args={{
      disabled: true,
      options: exampleOptions,
    }}
    parameters={{
      docs: {
        source: {
          state: "closed",
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Invalid

<Canvas>
  <Story
    name="Invalid select"
    args={{
      invalid: true,
      options: exampleOptions,
    }}
    parameters={{
      docs: {
        source: {
          state: "closed",
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Read-only

<Canvas>
  <Story
    name="Read-only select"
    args={{
      options: exampleOptions,
      readOnly: true,
    }}
    parameters={{
      docs: {
        source: {
          state: "closed",
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Required

<Canvas>
  <Story
    name="Required select"
    args={{
      options: exampleOptions,
      required: true,
    }}
    parameters={{
      docs: {
        source: {
          state: "closed",
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Interactive States

### Focus

<Canvas>
  <Story
    name="Select with focus"
    args={{
      focus: true,
      options: exampleOptions,
    }}
    parameters={{
      docs: {
        source: {
          state: "closed",
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
