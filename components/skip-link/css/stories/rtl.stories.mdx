<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, exampleArgs, SkipLink } from "../story-template";
import "../index.scss";

export const ExampleViewport = (Story) => (
  <div style={{ position: "relative" }} dir="rtl" lang="ar">
    {Story()}
  </div>
);
ExampleViewport.displayName = "ExampleViewport";

<Meta
  id="css-skip-link"
  title="CSS Component/Skip link/Right-to-left"
  argTypes={argTypes}
  decorators={[ExampleViewport]}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Skip link in right-to-left script

<Canvas>
  <Story
    name="Right-to-left"
    args={{
      ...exampleArgs,
      focus: true,
      textContent: "تخطي إلى المحتوى الرئيسي",
    }}
  >
    {SkipLink.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Right-to-left" />
