<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, exampleArgs, SkipLink } from "../story-template";
import "../index.scss";

<Meta
  id="css-skip-link"
  title="CSS Component/Skip link/Right-to-left"
  argTypes={argTypes}
  decorators={[
    (Story) => (
      <div dir="rtl" lang="ar">
        {Story()}
      </div>
    ),
  ]}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Skip link in right-to-left script

<Canvas>
  <Story
    name="Right-to-left"
    args={{
      ...exampleArgs,
      focus: true,
      textContent: "تخطي إلى المحتوى الرئيسي",
    }}
  >
    {SkipLink.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Right-to-left" />
