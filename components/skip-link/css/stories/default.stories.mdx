<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, exampleArgs, SkipLink } from "../story-template";
import "../index.scss";

export const ExampleViewport = (Story) => <div style={{ position: "relative" }}>{Story()}</div>;
ExampleViewport.displayName = "ExampleViewport";

<Meta
  id="css-skip-link"
  title="CSS Component/Skip link"
  argTypes={argTypes}
  decorators={[ExampleViewport]}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Skip link

Styling met de `.utrecht-skip-link` en `.utrecht-skip-link--visible-on-focus` class naam:

<Canvas>
  <Story
    name="Default"
    args={{
      ...exampleArgs,
      focus: true,
    }}
  >
    {SkipLink.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
