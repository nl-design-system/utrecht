{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, Heading4 } from "../story-template";

import "../index.scss";

<Meta
  id="css-heading-4--distanced"
  title="CSS Component/Heading/Heading 4/Distanced"
  component={Heading4}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Heading 4 distanced

Styling via `utrecht-heading-4` and `utrecht-heading-4--distanced` class names:

<Canvas>
  <Story
    name="Distanced"
    args={{
      ...exampleArgs,
      distanced: true,
    }}
  >
    {Heading4.bind({})}
  </Story>
</Canvas>
