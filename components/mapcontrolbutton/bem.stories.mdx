<!--
@license EUPL-1.2
Copyright (c) 2021 Gemeente Utrecht
Copyright (c) 2021 Robbert Broersma
-->

import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { defaultArgs, MapControlButton } from "./bem";
import "./bem.scss";

<Meta
  title="CSS Component/Map Control Button"
  argTypes={{
    focus: {
      description: "Focus",
      control: "boolean",
    },
    hover: {
      description: "Hover",
      control: "boolean",
    },
    disabled: {
      description: "Disabled",
      control: "boolean",
    },
    icon: {
      description: "Distance the element from adjacent content",
      control: { type: "select" },
      options: ["", "utrecht-icon-zoomin", "utrecht-icon-zoomout"],
    },
    label: {
      description: "Label",
      control: "text",
    },
  }}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => MapControlButton(args),
    },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button

Styling via the `.utrecht-mapcontrolbutton` class name:

<Canvas>
  <Story
    name="Map Control Button"
    args={{
      ...defaultArgs,
      icon: "utrecht-icon-zoomin",
    }}
  >
    {MapControlButton.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Map Control Button" />

Styling via the `.utrecht-mapcontrolbutton--hover` class name:

<Canvas>
  <Story
    name="Map Control Button hover"
    args={{
      hover: true,
      icon: "utrecht-icon-zoomin",
    }}
    parameters={{
      docs: {
        source: {
          state: "closed",
        },
      },
    }}
  >
    {MapControlButton.bind({})}
  </Story>
</Canvas>

Styling via the `.utrecht-mapcontrolbutton--focus` class name:

<Canvas>
  <Story
    name="Map Control Button focus"
    args={{
      focus: true,
      icon: "utrecht-icon-zoomin",
    }}
    parameters={{
      docs: {
        source: {
          state: "closed",
        },
      },
    }}
  >
    {MapControlButton.bind({})}
  </Story>
</Canvas>

Styling via the `.utrecht-mapcontrolbutton--disabled` class name:

<Canvas>
  <Story
    name="Map Control Button disabled"
    args={{
      disabled: true,
      icon: "utrecht-icon-zoomin",
    }}
    parameters={{
      docs: {
        source: {
          state: "closed",
        },
      },
    }}
  >
    {MapControlButton.bind({})}
  </Story>
</Canvas>

## Button with label

<Canvas>
  <Story
    name="Button with label"
    args={{
      icon: "utrecht-icon-filter",
      label: "Filter",
    }}
    parameters={{
      docs: {
        source: {
          state: "closed",
        },
      },
    }}
  >
    {MapControlButton.bind({})}
  </Story>
</Canvas>
