{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { MapControlButton } from "../story-template";

import "../index.scss";

<Meta
  id="css-map-control-button--label"
  title="CSS Component/Map Control Button/With label"
  component={MapControlButton}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button with label

<Canvas>
  <Story
    name="With label"
    args={{
      icon: "utrecht-icon-filter",
      label: "Filter",
    }}
    parameters={{
      docs: {
        source: {
          state: "closed",
        },
      },
    }}
  >
    {MapControlButton.bind({})}
  </Story>
</Canvas>
