<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, FormFieldset } from "../story-template";
import "../index.scss";

<Meta
  id="css-form-fieldset"
  title="CSS Component/Form fieldset"
  component={FormFieldset}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Invalid fieldset

Styling via the `.utrecht-form-fieldset__legend--invalid` class names:

<Canvas>
  <Story
    name="Invalid"
    args={{
      ...exampleArgs,
      invalid: true,
    }}
  >
    {FormFieldset.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Invalid" />
