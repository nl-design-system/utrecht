{/* @license CC0-1.0 */}

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, FormFieldset } from "../story-template";
import "../index.scss";

<Meta
  id="css-form-fieldset--disabled"
  title="CSS Component/Form fieldset"
  component={FormFieldset}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Disabled fieldset

Styling via the `.utrecht-form-fieldset__legend--disabled` class names:

<Canvas>
  <Story
    name="Disabled"
    args={{
      ...exampleArgs,
      disabled: true,
    }}
  >
    {FormFieldset.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Disabled" />
