<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, Code, defaultArgs, exampleArgs } from "../story-template";

<Meta
  id="html-code"
  title="HTML Component/Code"
  component={Code}
  argTypes={argTypes}
  args={defaultArgs}
  decorators={[(Story) => <div className="utrecht-html">{Story()}</div>]}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Code

Styling via the `code` HTML element:

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {Code.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
