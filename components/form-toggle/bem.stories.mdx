<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { defaultArgs, FormToggle, FormToggleCheckbox } from "./bem";
import "./bem.scss";

<Meta
  title="CSS Component/Form toggle"
  argTypes={{
    checked: {
      description: "Checked",
      control: "boolean",
    },
    disabled: {
      description: "Disabled",
      control: "boolean",
    },
    focus: {
      description: "Focus",
      control: "boolean",
    },
    hover: {
      description: "Hover",
      control: "boolean",
    },
  }}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => FormToggle(args),
    },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form toggle component

Styling via the `.utrecht-form-toggle` class name:

<Canvas>
  <Story
    name="Form toggle"
    args={{
      ...defaultArgs,
    }}
  >
    {FormToggle.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Form toggle" />

## Checked form toggle

<Canvas>
  <Story
    name="Checked form toggle"
    args={{
      ...defaultArgs,
      checked: true,
    }}
  >
    {FormToggle.bind({})}
  </Story>
</Canvas>

## Disabled form toggle

<Canvas>
  <Story
    name="Disabled form toggle"
    args={{
      ...defaultArgs,
      checked: true,
      disabled: true,
    }}
  >
    {FormToggle.bind({})}
  </Story>
</Canvas>

## Disabled checked form toggle

<Canvas>
  <Story
    name="Disabled checked form toggle"
    args={{
      ...defaultArgs,
      disabled: true,
    }}
  >
    {FormToggle.bind({})}
  </Story>
</Canvas>

## Toggle with invisible `<input type="checkbox">`

<Canvas>
  <Story
    name="Toggle with invisible checkbox input"
    args={{
      ...defaultArgs,
      id: "0e73edcc-6479-4a9f-9854-9669e5a79ee8",
    }}
    parameters={{
      docs: {
        transformSource: (_src, { args }) => FormToggleCheckbox(args),
      },
    }}
  >
    {FormToggleCheckbox.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Toggle with invisible checkbox input" />
