<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import "./bem.scss";

export const defaultArgs = {
  checked: false,
  disabled: false,
};

export const Template = ({ checked = false, disabled = false }) =>
  `<utrecht-form-toggle${checked ? ' checked="true"' : ""}${disabled ? ' disabled="true"' : ""}></utrecht-form-toggle>`;

<Meta
  title="Web Component/Form toggle"
  argTypes={{
    checked: {
      description: "Checked",
      control: "boolean",
    },
    disabled: {
      description: "Disabled",
      control: "boolean",
    },
  }}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form toggle component

<Canvas>
  <Story
    name="Form toggle"
    args={{
      ...defaultArgs,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Form toggle" />

## Checked form toggle

<Canvas>
  <Story
    name="Checked form toggle"
    args={{
      ...defaultArgs,
      checked: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled form toggle

<Canvas>
  <Story
    name="Disabled form toggle"
    args={{
      ...defaultArgs,
      disabled: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
