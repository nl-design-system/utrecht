{/* @license CC0-1.0 */}

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, LogoButton } from "../story-template";
import "../index.scss";

<Meta
  title="CSS Component/Logo Button"
  component={LogoButton}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button met logo

Styling via the `.utrecht-logo-button` class name:

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {LogoButton.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
