{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { LogoButton } from "../story-template";
import "../index.scss";

<Meta
  title="CSS Component/Logo Button/Right to left"
  component={LogoButton}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button met logo Right-to-left

<Canvas>
  <Story
    name="Right to left"
    decorators={[
      (story) => (
        <div dir="rtl" lang="ar">
          {story()}
        </div>
      ),
    ]}
    args={{
      label: "تسجيل الدخول باستخدام DigiD",
      textContent: "تسجيل الدخول",
      logo: "digid",
    }}
  >
    {LogoButton.bind({})}
  </Story>
</Canvas>
