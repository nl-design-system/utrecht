import { Canvas, Meta, Story } from "@storybook/blocks";
import clsx from "clsx";
import { Heading2 } from "../../../heading-2/css/story-template";
import { Paragraph } from "../../../paragraph/css/story-template";
import { Link } from "../story-template";
import "../index.scss";

export const Article = ({ children, className, ...restProps }, ref) => (
  <article {...restProps} ref={ref} className={clsx("utrecht-article", className)}>
    {children}
  </article>
);

<Meta
  id="css-link--tabindex"
  title="CSS Component/Link/Tabindex"
  component={Link}
  decorators={[
    (Story) => (
      <Article>
        <Heading2 id="heading">
          <Link href="htttps://www.example.com/">Puppy met zes poten</Link>
        </Heading2>
        <Paragraph>In de gemeente Monster is een puppy geboren met zes poten. {Story()}</Paragraph>
      </Article>
    ),
  ]}
  parameters={{
    status: {
      type: "ALPHA",
    },
    chromatic: { disableSnapshot: true },
  }}
/>

# Link met aangepaste tabindex

<Canvas>
  <Story
    name="Tabindex"
    args={{
      href: "https://www.example.com/",
      tabIndex: "-1",
      textContent: "Lees meer...",
      "aria-labelledby": "heading",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>
