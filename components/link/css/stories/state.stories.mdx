<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, Link } from "../story-template";
import "../index.scss";

<Meta
  id="css-link"
  title="CSS Component/Link/States"
  component={Link}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Link states

## Hover state

Styling met de `.utrecht-link--hover` class naam of als state op link met `.utrecht-link:hover`:

<Canvas>
  <Story
    name="hover"
    args={{
      ...exampleArgs,
      hover: true,
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

<ArgsTable story="hover" />

## Focus state

Focus kan op twee verschillende manieren worden aangebracht op een element. Met de `tab` knop van het keyboard die `.utrecht-link:focus` zet en
Styling met de `.utrecht-link--focus` class naam:

<Canvas>
  <Story
    name="focus"
    args={{
      ...exampleArgs,
      focus: true,
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

## Focus-visible state

Styling met de `.utrecht-link--focus-visible` class naam of als status op `.utrecht-link:focus-visible`:

Het is belangrijk dat de `focus-visible` staat duidelijk herkenbaar is (_distinguishable_). Een link met `hover` en een link met `focus-visible` moeten een ander design hebben.

Een link met `focus-visible` moet niet herkenbaar alleen zijn omdat die er anders uit ziet in een groep van links. Ook als een link met `focus-visible` de enige zichtbare link is, moet het duidelijk zijn dat de link focus heeft. De link alleen groter maken is bijvoorbeeld onvoldoende, omdat dit alleen herkenbaar is in een groep van links.

<Canvas>
  <Story
    name="focus-visible"
    args={{
      ...exampleArgs,
      focus: true,
      focusVisible: true,
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

### Active state

Styling met de `.utrecht-link--active` class naam of als status op `.utrecht-link:active`:

<Canvas>
  <Story
    name="active"
    args={{
      ...exampleArgs,
      active: true,
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

## Visited state

Styling met de `.utrecht-link--visited` class naam of als status op `.utrecht-link:visited`:

<Canvas>
  <Story
    name="visited"
    args={{
      ...exampleArgs,
      visited: true,
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>
