<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { ComponentTokensSection } from "../../documentation/components/ComponentTokensSection";
import tokens from "../../proprietary/design-tokens/dist/index.json";
import { defaultArgs, Link } from "./bem";
import tokensDefinition from "./tokens.json";
import "./bem.scss";

<Meta
  title="CSS Component/Link"
  argTypes={{
    active: {
      description: "Active",
      control: "boolean",
    },
    external: {
      description: "External",
      control: "boolean",
    },
    hover: {
      description: "Hover",
      control: "boolean",
    },
    focus: {
      description: "Focus",
      control: "boolean",
    },
    focusVisible: {
      description: "Focus visible",
      control: "boolean",
    },
    href: {
      description: "Target URL",
      type: {
        name: "string",
        required: true,
      },
    },
    telephone: {
      description: "Link to telephone number",
      control: "boolean",
    },
    textContent: {
      description: "Link text",
      type: {
        name: "string",
        required: true,
      },
    },
    visited: {
      description: "Visited",
      control: "boolean",
    },
  }}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Link

Styling met de `.utrecht-link` class naam:

<Canvas>
  <Story
    name="Link"
    args={{
      ...defaultArgs,
      href: "",
      textContent: "Voorbeeld van een link",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Link" />

## Link in plaats van `<a>`

Voor de component naam hebben we voor "`link`" gekozen, en niet voor "`a`" zoals de [`<a>`](https://html.spec.whatwg.org/multipage/text-level-semantics.html#the-a-element) in HTML. Buiten de context van HTML-code (bijvoorbeeld in Figma) is `a`niet duidelijk genoeg, en we verwachten niet dat er grote verwarring ontstaat met het [`<link>`](https://html.spec.whatwg.org/multipage/semantics.html#the-link-element) element van HTML.

## Algemene richtlijnen voor developers bij links

- Gebruik het `<a>` element om links aan te maken.
- Vermijd het gebruik van `href="#"`. Dit brengt de focus naar het begin van de pagina en dat is nooit wensbaar.
- Vermijd het gebruik van `tabindex="-1"` or `tabindex="0"` bij het `<a>` element. Links kunnen al standaard focus ontvangen.

## Link design tokens

Hieronder een overzicht van de design tokens die gebruikt worden in deze heading met de uiteindelijke waardes die ze gebruiken.

<ComponentTokensSection tokens={tokens} definition={tokensDefinition} component="utrecht-link"></ComponentTokensSection>

## Link met pijl

Styling met de `.utrecht-link-icon-left` class naam:

<Canvas>
  <Story
    name="Link met pijl"
    args={{
      iconleft: true,
      textContent: "Voorbeeld van een link",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

## States

Styling met de `.utrecht-link--hover` class naam:

### Hover state

<Canvas>
  <Story
    name="Link hover"
    args={{
      hover: true,
      textContent: "Voorbeeldlink",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

### Focus state

Focus kan op twee verschillende manieren worden aangebracht op een element. Met de `tab` knop van het keyboard en
Styling met de `.utrecht-link--focus` class naam:

<Canvas>
  <Story
    name="Link focus"
    args={{
      focus: true,
      textContent: "Voorbeeldlink",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

### Focus-visible state

Styling met de `.utrecht-link--focus-visible` class naam:

<Canvas>
  <Story
    name="Link focus-visible"
    args={{
      focus: true,
      focusVisible: true,
      textContent: "Voorbeeldlink",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

### Active state

Styling met de `.utrecht-link--active` class naam:

<Canvas>
  <Story
    name="Link active"
    args={{
      active: true,
      textContent: "Voorbeeldlink",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

### Visited state

Styling met de `.utrecht-link--visited` class naam:

<Canvas>
  <Story
    name="Link visited"
    args={{
      textContent: "Voorbeeldlink",
      visited: true,
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

## Link naar telefoonnummer

Styling met de `.utrecht-link--telephone` class naam. We gebruiken `white-space: nowrap` om line wrapping te voorkomen bij een telefoonnummer, zodat deze goed leesbaar blijft.

<Canvas>
  <Story
    name="Link naar telefoonnummer"
    args={{
      href: "tel:+31555385385",
      textContent: "+31 555 385 385",
      telephone: true,
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

## E-mail link

Styling met de `.utrecht-link--email` class naam.

<Canvas>
  <Story
    name="E-mail link"
    args={{
      href: "mailto:info@example.com",
      textContent: "info@example.com",
      mail: true,
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

## Externe link

Styling met de `.utrecht-link--external` class naam.

<Canvas>
  <Story
    name="Externe link"
    args={{
      external: true,
      href: "https://www.whatismyreferer.com",
      textContent: "Voorbeeldlink",
    }}
  >
    {Link.bind({})}
  </Story>
</Canvas>

- `rel="external"` is old-school metadata without any particular effect, but it is a sementically valid way help identify external links with the `a[rel~="external"]`.
- `rel="noopener"` helps security: disable JavaScript contact via `window.opener` between the current page and the page you link to.
- `rel="noreferer"` helps privacy: normal links leak privacy sensitive data. The 3rd-party website you link to will find out what page you were on via the HTTP `Referer` header. Including `noreferer` disables the `Referer` header.
- Avoid using `target="_blank"` for external just because you don't want the visitor to leave your website, they can decide themselves to open a link in a new tab or window.

## Link in het Arabisch

De underline van een link mag nooit een negatieve impact hebben op de leesbaarheid van de tekst door het doorkruisen van [Arabische diakrische tekens](https://en.wikipedia.org/wiki/Arabic_diacritics). Met de waarde van `text-decoration-skip-ink` in CSS kan de leesbaarheid vergroot worden door de underline niet over diakrieten heen te laten gaan.

<Canvas>
  <Story
    name="Link in het Arabisch"
    args={{
      href: "mailto:info@example.com",
      textContent: "تواصل معنا من خلال البريد الإلكتروني",
      mail: true,
    }}
    decorators={[(story) => `<div dir="rtl" lang="ar">${story()}</div>`]}
  >
    {Link.bind({})}
  </Story>
</Canvas>
