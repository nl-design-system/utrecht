{/* @license CC0-1.0 */}

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, ToptaskNav } from "../story-template";
import "../index.scss";

<Meta
  id="css-toptask-nav"
  title="CSS Component/Toptask Navigation"
  component={ToptaskNav}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Toptask Navigation

Styling via the `.utrecht-toptask-nav` class name:

<Canvas>
  <Story
    name="Default"
    args={exampleArgs}
    parameters={{
      viewport: {
        viewports: {
          example: {
            name: "Example",
            styles: {
              width: "400px",
              height: "400px",
            },
          },
        },
        defaultViewport: "example",
      },
    }}
  >
    {ToptaskNav.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
