{/* @license CC0-1.0 */}

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, ToptaskLink } from "../story-template";
import "../index.scss";

<Meta
  id="css-toptask-link"
  title="CSS Component/Toptask Link"
  component={ToptaskLink}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Toptask Link

Styling via the `.utrecht-toptask-link` class name:

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {ToptaskLink.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
