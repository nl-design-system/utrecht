{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, ToptaskLink } from "../story-template";
import "../index.scss";

<Meta
  id="css-toptask-link"
  title="CSS Component/Toptask Link/State"
  component={ToptaskLink}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Toptask Link states

## Hover

Styling via the `.utrecht-toptask-link--hover` class name:

<Canvas>
  <Story
    name="Hover"
    args={{
      ...exampleArgs,
      hover: true,
    }}
  >
    {ToptaskLink.bind({})}
  </Story>
</Canvas>

## Focus

Styling via the `.utrecht-toptask-link--focus` class name:

<Canvas>
  <Story
    name="focus"
    args={{
      ...exampleArgs,
      focus: true,
    }}
  >
    {ToptaskLink.bind({})}
  </Story>
</Canvas>

## Focus visible

Styling via the `.utrecht-toptask-link--focus-visible` class name:

<Canvas>
  <Story
    name="focus-visible"
    args={{
      ...exampleArgs,
      focus: true,
      focusVisible: true,
    }}
  >
    {ToptaskLink.bind({})}
  </Story>
</Canvas>
