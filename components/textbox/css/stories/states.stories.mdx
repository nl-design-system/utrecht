<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, Textbox } from "../story-template";
import "../index.scss";

<Meta
  id="css-textbox"
  title="CSS Component/Textbox/States"
  component={Textbox}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Text box component states

## Disabled

Gebruik de `utrecht-textbox--disabled` classname.

<Canvas>
  <Story
    name="disabled"
    args={{
      ...exampleArgs,
      disabled: true,
    }}
  >
    {Textbox.bind({})}
  </Story>
</Canvas>

## Invalid

Gebruik de `utrecht-textbox--invalid` classname.

Gebruik `aria-invalid="true"` wanneer je client-side of server-side validatie doet. Zie ook: [WCAG Technique: Using `aria-invalid` to Indicate An Error Field](https://www.w3.org/WAI/WCAG21/Techniques/aria/ARIA21). Maak ook duidelijk hoe de gebruiker de fout kan verhelpen, door een _invalid form field description_ te koppelen aan de textbox.

In CSS werkt `:invalid` niet voor `aria-invalid="true"`, gebruik daarom `.utrecht-textbox:invalid, .utrecht-textbox[aria-invalid="true"]` om beide varianten te herkennen.

<Canvas>
  <Story
    name="invalid"
    args={{
      ...exampleArgs,
      invalid: true,
    }}
  >
    {Textbox.bind({})}
  </Story>
</Canvas>

## Read-only

<Canvas>
  <Story
    name="read-only"
    args={{
      ...exampleArgs,
      readOnly: true,
    }}
  >
    {Textbox.bind({})}
  </Story>
</Canvas>

## Required

<Canvas>
  <Story
    name="required"
    args={{
      ...exampleArgs,
      required: true,
    }}
  >
    {Textbox.bind({})}
  </Story>
</Canvas>

## Focus

Gebruik de `utrecht-textbox--focus` classname om het `:focus` design te testen.

<Canvas>
  <Story
    name="focus"
    args={{
      ...exampleArgs,
      focus: true,
    }}
  >
    {Textbox.bind({})}
  </Story>
</Canvas>
