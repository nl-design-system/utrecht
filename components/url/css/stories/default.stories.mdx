<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, URLTemplate } from "../story-template";
import "../index.scss";

<Meta
  id="css-url"
  title="CSS Component/URL"
  component={URLTemplate}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    // This story is rendered as regular text, no testing required
    chromatic: { disableSnapshot: true },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# URL

<Canvas>
  <Story args={exampleArgs} name="Default">
    {URLTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
