<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import "./bem.scss";

export const defaultArgs = {
  busy: false,
  icon: "",
  iconPosition: "",
  disabled: false,
  textContent: "",
  type: "",
};

export const Template = ({ busy = false, disabled = false, icon = "", textContent = "", type = "" }) =>
  `<utrecht-button${busy ? ' busy="true"' : ""}${disabled ? " disabled" : ""}${
    type ? ` type="${type}"` : ""
  }>${textContent}${icon ? `<${icon}></${icon}>` : ""}</utrecht-button>`;

<Meta
  title="Web Component/Button"
  argTypes={{
    textContent: {
      description: "Button text",
      control: "text",
    },
    busy: {
      description: "Busy",
      control: "boolean",
    },
    disabled: {
      description: "Disabled",
      control: "boolean",
    },
    icon: {
      description: "Icon",
      type: {
        name: "enum",
      },
      options: ["", "utrecht-icon-arrow-left", "utrecht-icon-arrow-right"],
    },
    type: {
      description: "Type",
      type: {
        name: "enum",
      },
      options: ["", "button", "reset", "submit"],
    },
  }}
  parameters={{
    docs: {
      transformSource: (_src, { args }) => Template(args),
    },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button

<Canvas>
  <Story
    name="Button"
    args={{
      ...defaultArgs,
      textContent: "Read more...",
      type: "",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Button" />

<Canvas>
  <Story
    name="Submit button"
    args={{
      ...defaultArgs,
      textContent: "Send",
      type: "submit",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## State

### Busy

<Canvas>
  <Story
    name="Busy button"
    args={{
      ...defaultArgs,
      busy: true,
      textContent: "Send",
      type: "submit",
    }}
    parameters={{
      // `cursor` cannot be tested using visual regression testing, ignore this story
      percy: { skip: true },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Actions

### Primary

<Canvas>
  <Story
    name="Primary action"
    args={{
      ...defaultArgs,
      textContent: "Primary action",
      action: "primary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Secondary

<Canvas>
  <Story
    name="Secondary action"
    args={{
      ...defaultArgs,
      textContent: "Secondary action",
      action: "secondary",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Danger

<Canvas>
  <Story
    name="Dangerous action"
    args={{
      ...defaultArgs,
      textContent: "Dangerous action",
      action: "danger",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Next step

<Canvas>
  <Story
    name="Next step"
    args={{
      ...defaultArgs,
      textContent: "Previous",
      action: "primary",
      icon: "utrecht-icon-arrow-left",
      iconLocation: "start",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

### Previous step

<Canvas>
  <Story
    name="Previous step"
    args={{
      ...defaultArgs,
      textContent: "Next",
      action: "secondary",
      icon: "utrecht-icon-arrow-right",
      iconLocation: "end",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
