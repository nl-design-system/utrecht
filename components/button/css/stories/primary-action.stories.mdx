<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Button } from "../story-template";
import "../index.scss";

<Meta
  id="css-button"
  title="CSS Component/Button/appearance"
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
      // This story is already rendered in the appearance grid, no separate testing required
      chromatic: { disableSnapshot: true },
    },
  }}
/>

# Button

Styling via the `.utrecht-button` with `.utrecht-button--primary-action` modifier class names:

<Canvas>
  <Story
    name="primary-action"
    args={{
      appearance: "primary-action-button",
      textContent: "Primary action button",
      type: "button",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>
