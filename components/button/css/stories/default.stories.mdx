<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, Button, defaultArgs, exampleArgs } from "../story-template";
import "../index.scss";

<Meta
  id="css-button"
  title="CSS Component/Button"
  component={Button}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button

Styling via the `.utrecht-button` class name:

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {Button.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
