<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Button } from "../story-template";
import "../index.scss";

<Meta
  id="css-button"
  title="CSS Component/Button/appearance"
  component={Button}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
    // This story is already rendered in the appearance grid, no separate testing required
    chromatic: { disableSnapshot: true },
  }}
/>

# Button

Styling via the `.utrecht-button` with `.utrecht-button--secondary-action` modifier class names:

<Canvas>
  <Story
    name="secondary-action"
    args={{
      appearance: "secondary-action-button",
      textContent: "Secondary action button",
      type: "button",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>
