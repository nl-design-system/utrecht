{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Button } from "../story-template";
import "../index.scss";

<Meta
  id="css-button--subtle-button"
  title="CSS Component/Button/appearance"
  component={Button}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button

Styling via the `.utrecht-button` with `.utrecht-button--subtle` modifier class names:

<Canvas>
  <Story
    name="subtle"
    args={{
      appearance: "subtle-button",
      textContent: "Subtle button",
      type: "button",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>
