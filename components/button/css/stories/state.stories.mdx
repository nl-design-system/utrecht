<!-- @license CC0-1.0 -->

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Button, defaultArgs } from "../story-template";
import "../index.scss";

<Meta
  id="css-button--state"
  title="CSS Component/Button/States"
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Button

## States

### Active

Styling via the `.utrecht-button--active` class name:

<Canvas>
  <Story
    name="active"
    args={{
      ...defaultArgs,
      active: true,
      textContent: "Read more...",
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

### Hover

Styling via the `.utrecht-button--hover` class name:

<Canvas>
  <Story
    name="hover"
    args={{
      ...defaultArgs,
      textContent: "Read more...",
      hover: true,
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

### Focus

Styling via the `.utrecht-button--focus` class name:

<Canvas>
  <Story
    name="focus"
    args={{
      ...defaultArgs,
      textContent: "Read more...",
      focus: true,
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

### Focus visible

Styling via the `.utrecht-button--focus-visible` class name:

<Canvas>
  <Story
    name="focus visible"
    args={{
      ...defaultArgs,
      textContent: "Read more...",
      focus: true,
      focusVisible: true,
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

### Disabled

Styling via the `.utrecht-button--disabled` class name:

<Canvas>
  <Story
    name="disabled"
    args={{
      ...defaultArgs,
      textContent: "Read more...",
      disabled: true,
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>

### Busy

Styling via the `.utrecht-button--busy` class name:

<Canvas>
  <Story
    name="busy"
    args={{
      ...defaultArgs,
      busy: true,
      textContent: "Read more...",
    }}
    parameters={{
      // `cursor` cannot be tested using visual regression testing, ignore this story
      percy: { skip: true },
    }}
  >
    {Button.bind({})}
  </Story>
</Canvas>
