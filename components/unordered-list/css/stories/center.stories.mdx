{/* @license CC0-1.0 */}

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, UnorderedList } from "../story-template";
import "../index.scss";

<Meta
  id="css-unordered-list"
  title="CSS Component/Unordered List/Center"
  component={UnorderedList}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

_Unordered list_ moet niet als gecentreerde tekst gebruikt worden worden. Wanneer het niet te vermijden is, dan moeten marker dicht naast de tekst staan, niet in de marge.

<Canvas>
  <Story
    name="Center"
    args={{
      center: true,
      items: [
        {
          children: "The Quick Brown Fox",
        },
        {
          children: "Jumps",
        },
        {
          children: "Over The Lazy Dog",
        },
      ],
    }}
    decorators={[(Story) => <div style={{ "text-align": "center" }}>{Story()}</div>]}
  ></Story>
</Canvas>

<ArgsTable story="Center" />
