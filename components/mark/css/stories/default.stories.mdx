{/* @license CC0-1.0 */}

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Paragraph } from "../../../paragraph/css/story-template";
import { argTypes, defaultArgs, exampleArgs, Mark } from "../story-template";
import "../index.scss";

<Meta
  id="css-mark"
  title="CSS Component/Mark"
  component={Mark}
  argTypes={argTypes}
  args={defaultArgs}
  decorators={[(Story) => <Paragraph>{Story()}</Paragraph>]}
  parameters={{
    status: {
      type: "ALPHA",
    },
  }}
/>

# Mark

Styling via `utrecht-mark` class name:

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {Mark.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
