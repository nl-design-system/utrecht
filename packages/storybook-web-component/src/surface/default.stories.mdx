<!-- @license CC0-1.0 -->

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, defaultArgs, exampleArgs, Surface } from "./story-template";

<Meta
  id="web-component-surface"
  title="Web Component/Surface"
  component={Surface}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    layout: "fullscreen",
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Surface component

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {Surface.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
