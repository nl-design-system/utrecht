{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { exampleArgs, FormFieldCheckbox } from "./story-template";

<Meta
  id="web-component-form-field-checkbox"
  title="Web Component/Form field with checkbox/States"
  component={FormFieldCheckbox}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Form field with checkbox states

## Checked

<Canvas>
  <Story
    name="checked"
    args={{
      ...exampleArgs,
      checked: true,
    }}
  >
    {FormFieldCheckbox.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story
    name="disabled"
    args={{
      ...exampleArgs,
      disabled: true,
    }}
  >
    {FormFieldCheckbox.bind({})}
  </Story>
</Canvas>

## Invalid

<Canvas>
  <Story
    name="invalid"
    args={{
      ...exampleArgs,
      invalid: true,
    }}
  >
    {FormFieldCheckbox.bind({})}
  </Story>
</Canvas>

## Required

<Canvas>
  <Story
    name="required"
    args={{
      ...exampleArgs,
      required: true,
    }}
  >
    {FormFieldCheckbox.bind({})}
  </Story>
</Canvas>
