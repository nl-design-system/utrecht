{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/blocks";
import { argTypes, defaultArgs, LogoButton } from "./story-template";

<Meta
  title="Web Component/Logo Button/Logos"
  id="web-component-logo-button"
  component={LogoButton}
  args={defaultArgs}
  argTypes={argTypes}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# DigiD Logo Button

<Canvas>
  <Story name="Digid">{LogoButton.bind({})}</Story>
</Canvas>

# Eherkenning Logo Button

<Canvas>
  <Story name="eherkenning" args={{ logo: "eherkenning" }}>
    {LogoButton.bind({})}
  </Story>
</Canvas>

# Eidas Logo Button

<Canvas>
  <Story name="eidas" args={{ logo: "eidas" }}>
    {LogoButton.bind({})}
  </Story>
</Canvas>
