{/* @license CC0-1.0 */}

import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { argTypes, BadgeStatus, defaultArgs, exampleArgs } from "./story-template";

<Meta
  id="web-component-status-badge"
  title="Web Component/Badge/Status Badge"
  component={BadgeStatus}
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Status badge

When status is not provided, neutral will be used as default

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {BadgeStatus.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
