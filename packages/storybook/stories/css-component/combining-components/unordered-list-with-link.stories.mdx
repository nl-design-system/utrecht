{/* @license CC0-1.0 */}

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Link } from "@utrecht/components/link/css/story-template";
import { UnorderedList } from "@utrecht/components/unordered-list/css/story-template";
import * as ReactDOMServer from "react-dom/server";

<Meta
  title="CSS Component/Combining components/Link"
  component={UnorderedList}
  parameters={{
    chromatic: { disableSnapshot: true },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

# Link with other components

## Unordered list with link

<Canvas>
  <Story
    name="Unordered list with multiple links"
    args={{
      items: [
        {
          innerHTML: ReactDOMServer.renderToStaticMarkup(Link({ href: "https://example.com/", textContent: "Lorem" })),
        },
        {
          innerHTML: ReactDOMServer.renderToStaticMarkup(
            Link({ href: "https://example.com/", textContent: "ipsum dolor" })
          ),
        },
        {
          innerHTML: ReactDOMServer.renderToStaticMarkup(
            Link({ href: "https://example.com/", textContent: "sit amet" })
          ),
        },
      ],
    }}
  >
    {UnorderedList.bind({})}
  </Story>
</Canvas>

## Unordered list item with multiple links

<Canvas>
  <Story
    name="Unordered list item with multiple links"
    args={{
      items: [
        {
          innerHTML: ReactDOMServer.renderToStaticMarkup(
            <>
              {Link({ href: "https://example.com/", textContent: "Lorem" })}
              <br />
              {Link({ href: "https://example.com/", textContent: "ipsum dolor" })}
              <br />
              {Link({ href: "https://example.com/", textContent: "sit amet" })}
            </>
          ),
        },
      ],
    }}
  >
    {UnorderedList.bind({})}
  </Story>
</Canvas>
