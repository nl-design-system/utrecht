import React from 'react'

// Default implementation, that you can customize
function Root ({ children }) {
  return (
    <div className='utrecht-theme' lang='nl'>
      {children}
    </div>
  )
}

export default Root
