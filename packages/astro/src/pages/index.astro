---
import Layout from '../layouts/Layout.astro';
import { base } from 'astro:config/client';
import { joinUrl } from '../utils/url';
import { ButtonLink } from '@utrecht/component-library-react/css-module'
import TileNavigation from '../components/TileNavigation.astro';
import TileList from '../components/TileList.astro';
---

<Layout>
  <section>
    <div class="utrecht-hero">
      <h1 class="utrecht-heading-1">Utrecht Design System</h1>
      <p class="nl-paragraph">Principes, interactiepatronen, basiselementen en componenten</p>
      <div class="utrecht-hero__action-group">
        <ButtonLink appearance="primary-action-button" href={joinUrl(base, '/design-system')}>Getting started</ButtonLink>
        <ButtonLink appearance="secondary-action-button" href={joinUrl(base, '/componenten')}>Naar de componenten</ButtonLink>
      </div>
    </div>
  </section>
  <section>
    <h2>Onderwerpen</h2>
    <TileList>
      <TileNavigation
        href={joinUrl(base, '/design-system')}
        title="Utrecht Design System"
        description="Meer weten over het Utrecht Design System? Voor wie is het design system bedoeld?"
      />
      <TileNavigation
        href={joinUrl(base, '/richtlijnen')}
        title="Richtlijnen"
        description="Hoe ga ik als ontwikkelaar aan de slag gaan met Utrecht Design System?"
      />
      <TileNavigation
        href={joinUrl(base, '/developers')}
        title="Ontwikkelaars"
        description="Hoe ga ik als ontwikkelaar aan de slag gaan met Utrecht Design System?"
      />
      <TileNavigation
        href={joinUrl(base, '/designers')}
        title="Designers"
        description="Hoe ga ik als designer aan de slag met Utrecht Design System?"
      />
      <TileNavigation
        href={joinUrl(base, '/componenten')}
        title="Components"
        description="Hoe ga ik als designer aan de slag met Utrecht Design System?"
      />
      <TileNavigation
        href={joinUrl(base, '/tokens')}
        title="Tokens"
        description="Hoe ga ik als designer aan de slag met Utrecht Design System?"
      />
    </TileList>
  </section>
</Layout>

<style>
  .utrecht-hero {
    --utrecht-heading-1-color: white;

    background-color: #cc0000;
    background-image: var(--utrecht-page-footer-background-image);
    margin-inline-start: calc(-1 * var(--utrecht-page-body-content-padding-inline-start));
    margin-inline-end: calc(-1 * var(--utrecht-page-body-content-padding-inline-end));
    padding-block-start: 64px;
    padding-block-end: 64px;
    padding-inline-start: var(--utrecht-page-body-content-padding-inline-start);
    padding-inline-end: var(--utrecht-page-body-content-padding-inline-end);
    color: white;
  }

  .utrecht-hero__action-group {
    display: flex;
    flex-direction: row;
    column-gap: 16px;
  }
