import { action } from "@storybook/addon-actions";
import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Select } from "@utrecht/component-library-vue";
import { argTypes, options } from "./argTypes.ts";

<Meta id="vue-select" title="Vue.js Component/Select" component={Select} argTypes={argTypes} />

# Select

export const Template = (args) => ({
  components: { Select },
  setup() {
    return { args };
  },
  template: `
  <Select v-bind="args" @update:modelValue="action"/>`,
  methods: {
    action: action("modelValue"),
  },
});

<Canvas>
  <Story
    name="Default"
    args={{ options }}
    parameters={{
      docs: {
        source: {
          code: `<Select :options="options" />`,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Select} />
