import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { PageContent } from "@utrecht/component-library-vue";

<Meta id="vue-page-content" title="Vue.js Component/Page Content" component={PageContent} />

# PageContent Component

export const Template = (args) => ({
  components: { PageContent },
  setup() {
    return { args };
  },
  template: `<PageContent v-bind="args">${args.pageContent}</PageContent>`,
});

<Canvas>
  <Story
    name="PageContent"
    args={{ pageContent: "Page Content" }}
    parameters={{
      docs: {
        source: {
          code: `<PageContent>Page Content</PageContent>`,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="PageContent" />
