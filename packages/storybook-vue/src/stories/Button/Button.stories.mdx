import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Button } from "@utrecht/component-library-vue";
import argTypes from "./argTypes.ts";

<Meta id="vue-button" title="Vue.js Component/Button" component={Button} argTypes={argTypes} />

# Button

export const Template = (args) => ({
  components: { Button },
  setup() {
    return { args };
  },
  template: `<Button v-bind="args">${args.default}</Button>`,
});

<Canvas>
  <Story
    name="Button"
    args={{
      appearance: "primary-action-button",
      default: "Button",
      busy: false,
      disabled: false,
    }}
    parameters={{
      docs: { source: { code: `<Button appearance="primary-action-button">Primary Button</Button>` } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Button} />
