import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Button } from "@utrecht/component-library-vue";
import argTypes from "./argTypes.ts";

<Meta id="vue-button-appearance" title="Vue.js Component/Button/Apearance" component={Button} argTypes={argTypes} />

# Button Apearance

export const Template = (args) => ({
  components: { Button },
  setup() {
    return { args };
  },
  template: `<Button v-bind="args">${args.default}</Button>`,
});

<Canvas>
  <Story
    name="Primary Button"
    args={{
      appearance: "primary-action-button",
      default: "Primary Button",
      busy: false,
      disabled: false,
    }}
    parameters={{
      docs: { source: { code: `<Button appearance="primary-action-button">Primary Button</Button>` } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Secondary Button"
    args={{
      appearance: "secondary-action-button",
      default: "Secondary Button",
      busy: false,
      disabled: false,
    }}
    parameters={{
      docs: { source: { code: `<Button appearance="secondary-action-button">Secondary Button</Button>` } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Subtle Button"
    args={{
      appearance: "subtle-button",
      default: "Subtle Button",
      busy: false,
      disabled: false,
    }}
    parameters={{
      docs: { source: { code: `<Button appearance="subtle-button">Subtle Button</Button>` } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
