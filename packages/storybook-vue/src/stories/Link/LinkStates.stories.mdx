import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Link } from "@utrecht/component-library-vue";
import { argTypes, componentArgs } from "./argTypes.ts";

<Meta id="vue-link--state" title="Vue.js Component/Link/States" component={Link} argTypes={argTypes} />

# Link States

export const Template = (args) => ({
  components: { Link },
  setup() {
    return { args };
  },
  template: `<Link v-bind="args">${args.textContent}</Link>`,
});

## Hover

<Canvas>
  <Story
    name="Hover"
    args={{ ...componentArgs, textContent: "Link Hover" }}
    parameters={{
      pseudo: { hover: true },
      docs: { source: { code: `<Link>Link Hover</Link>` } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Focus

<Canvas>
  <Story
    name="Focus"
    args={{ ...componentArgs, textContent: "Link Focus" }}
    parameters={{
      pseudo: { focus: true },
      docs: { source: { code: `<Link>Link Focus</Link>` } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Focus Visible

<Canvas>
  <Story
    name="Focus Visible"
    args={{ ...componentArgs, textContent: "Link Focus Visible" }}
    parameters={{
      pseudo: { focusVisible: true },
      docs: { source: { code: `<Link>Link Focus Visible</Link>` } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Active

<Canvas>
  <Story
    name="Active"
    args={{ ...componentArgs, textContent: "Link Active" }}
    parameters={{
      pseudo: { active: true },
      docs: { source: { code: `<Link>Link Active</Link>` } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Visited

<Canvas>
  <Story
    name="Visited"
    args={{ ...componentArgs, textContent: "Link Visited" }}
    parameters={{
      pseudo: { visited: true },
      docs: { source: { code: `<Link>Link Visited</Link>` } },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
